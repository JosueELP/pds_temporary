<%= form_with(model: branch_office, local: true) do |form| %>
  <% if branch_office.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(branch_office.errors.count, "error") %> prohibited this branch_office from being saved:</h2>

      <ul>
      <% branch_office.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre_sucursal %>
    <%= form.text_field :branch_office_name %>
  </div>

  <div class="field">
    <%= form.label :teléfono%>
    <%= form.number_field :branch_office_phone %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :branch_office_email %>
  </div>

  <div>
    <div class="field">
      <p style="color:gray;">Dirección</p>
    </div>
    <div class="dropdown">
      <a class="btn btn-info dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Seleccione una Dirección</a>
      <div class="dropdown-menu">
        <% @address.each do |address| %>
          <div class="col">
            <%= form.radio_button :address_id, address.id %>
            <%= form.label :address_id, address.street + ", " + address.number.to_s + ", " + address.colony + ", " + address.postal_code.to_s, :value => address.id, :class=>"fieldTitle tableButton"%>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <div class="actions">
    <%= form.submit"Guardar",class:"btn bigBtn" %>
  </div>
<% end %>
