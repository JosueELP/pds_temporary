<%= render "partials/nav"%>
<div class="container">
  <div class="row">
    <div class="col">
      <h1>Consulta de Paquete</h1>
    </div>
  </div>

  <p id="notice"><%= notice %></p>

  <p class="tableInfo">
    <strong class="tableTitle">Cantidad:</strong>
    <%= @package.quantity %>
  </p>

  <p class="tableInfo">
    <strong class="tableTitle">Descripción:</strong>
    <%= @package.description %>
  </p>

  <%= link_to 'Editar', edit_package_path(@package), class:"tableButton" %> |
  <%= link_to 'Atrás', packages_path, class:"tableButton" %>

</div>
