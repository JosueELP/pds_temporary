<%= form_with(model: package, local: true) do |form| %>
  <% if package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(package.errors.count, "error") %> prohibited this package from being saved:</h2>

      <ul>
      <% package.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cantidad %>
    <%= form.number_field :quantity %>
  </div>


  
    <div>
    <div class="field">
      <p style="color:gray;">paquete</p>
    </div>
    <div class="dropdown">
      <a class="btn btn-info dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Seleccione una Dirección</a>
      <div class="dropdown-menu">
        <% @shippings.each do |shippings| %>
          <div class="col">
            <%= form.radio_button :shipping_id, shipping.id %>
            <%= form.label :shipping_id, shipping.id, :value => shipping.id, :class=>"fieldTitle tableButton"%>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <div class="field">
    <%= form.label :descripción %>
    <%= form.text_field :description %>
  </div>
  <div class="actions">
    <%= form.submit"Guardar",class:"btn bigBtn" %>
  </div>

<% end %>
