<%= form_with(model: billing_information, local: true) do |form| %>
  <% if billing_information.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(billing_information.errors.count, "error") %> prohibited this billing_information from being saved:</h2>

      <ul>
      <% billing_information.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :rfc %>
    <%= form.text_field :rfc %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="actions">
    <%= form.submit"Guardar",class:"btn bigBtn" %>
  </div>
<% end %>
